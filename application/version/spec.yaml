tables:
  center_group_hierarchy:

  language:

  group_global:
    - center

  group_region:
    lookup_tables:
      - group_global
      - center

  group_country:
    lookup_tables:
      - group_region
      - center

  group_cohort:
    lookup_tables:
      - group_country
      - center

  group_cluster:
    lookup_tables:
      - group_cohort 
      - center  

  group_cluster_user:
    lookup_tables:
      - user
      - group_cluster

  group_cohort_user:
    lookup_tables:
      - user
      - group_cohort

  group_country_user:
    lookup_tables:
      - user
      - group_country

  group_region_user:
    lookup_tables:
      - user
      - group_region

  group_global_user:
    lookup_tables:
      - user
      - group_global   

  department:
    lookup_tables:
      - approval
      - status

  department_user:
    lookup_tables:
      - user
      - department
      - approval
      - status
  
  designation:
    lookup_tables:
      - center_group_hierarchy 

  page_view:
    lookup_tables:
      - menu

  page_view_condition:
    lookup_tables:
      - page_view    
  
  page_view_role:
    lookup_tables:
      - role
      - page_view    

  request_type:

  group_center:
    lookup_tables:
      - group_cluster

  group_center_user:
    - user
    - group_center
    - designation

  group_cluster_unit:
    - center
    - group_cluster

  group_cohort_unit:
    - center
    - group_cohort
    
  group_country_unit:
    - center
    - group_country
  
  group_region_unit:
    - center
    - group_region
  
  group_global_unit:
    - center
    - group_global  

  group_center_unit:
    - center  



  