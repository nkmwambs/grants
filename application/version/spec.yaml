tables:
  center_group:
    lookup_tables:
      - center_group_hierarchy

  center_group_link:
    lookup_tables:
      - center
      - center_group

  center_group_hierarchy:

  language:

  group_global:
    - center

  group_region:
    lookup_tables:
      - group_global
      - center

  group_country:
    lookup_tables:
      - group_region
      - center

  group_cohort:
    lookup_tables:
      - group_country
      - center

  group_cluster:
    lookup_tables:
      - group_cohort 
      - center  

  center_user:
    lookup_tables:
      - center
      - user 

  group_cluster_user:
    lookup_tables:
      - user
      - group_cluster

  group_cohort_user:
    lookup_tables:
      - user
      - group_cohort

  group_country_user:
    lookup_tables:
      - user
      - group_country

  group_region_user:
    lookup_tables:
      - user
      - group_region

  group_global_user:
    lookup_tables:
      - user
      - group_global   

  department:
    lookup_tables:
      - approval
      - status

  department_user:
    lookup_tables:
      - user
      - department
      - approval
      - status
  
  designation:
    lookup_tables:
      - center_group_hierarchy 

  page_view:
    lookup_tables:
      - menu

  page_view_condition:
    lookup_tables:
      - page_view    
  
  page_view_role:
    lookup_tables:
      - role
      - page_view    

  request_type:

  group_center:
    lookup_tables:
      - group_cluster
      - center

  group_center_user:
    - user
    - group_center
    - designation


  